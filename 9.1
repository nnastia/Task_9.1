let calculator = {
 function read(){
    this.a=prompt("Write number value", 0);
    this.b=prompt("Write number value", 0);
    }
  function sum(){
      let sum = this.a + this.b;
      return(sum);
    }
   function mul(){
      let mul = this.a * this.b;
      return(mul);
    }
    }

let obj = {}
function A(){}
function B(){}
let a = new A;
let b = new B;
let a==b;

funclion Constructor = {startingValue}
this.value = startingValue;
this.read = function(){
this.value += prompt("Write number to add", 0);
}
